---
inject: true
to: "src/screens/index.<%= ts ? 'ts' : 'js' %>"
after: function registerScreens
---
<%
  var ScreenName = h.inflection.camelize(screenName)
-%>
  Navigation.registerComponent('<%= ScreenName %>', () => require('./<%= ScreenName %>').<%= ScreenName %>);
